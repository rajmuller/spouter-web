import { FC } from "react";
import { Box } from "@chakra-ui/core";

// type NavbarProps = {};

const Navbar: FC = () => {
  return (
    <Box d="flex" bg="cyan.700">
      Navbar
    </Box>
  );
};

export default Navbar;
